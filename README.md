```python
The main experiment compares SR-LoRA, contrastive LoRA, and plain LoRA across several datasets using the same Qwen model and judge. It evaluates which method improves hallucination performance most effectively.
```

### How to Run ###
```python
open terminal_code.txt in directory , copy and paster it in terminal and run.
```
